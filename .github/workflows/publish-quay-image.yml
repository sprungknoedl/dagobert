name: Publish Quay image

on:
  release:
    types: [published]
  push:
    branches: ['main']

jobs:
  build-web:
    name: Build and push image
    runs-on: ubuntu-22.04
    steps:
    - name: Check out the repo
      uses: actions/checkout@v4

    - name: Build Image (dagobert web)
      id: build-image
      uses: redhat-actions/buildah-build@v2
      with:
        image: dagobert
        tags: latest ${{ github.sha }}
        containerfiles: ./configs/Dockerfile

    - name: Push To quay.io
      id: push-to-quay
      uses: redhat-actions/push-to-registry@v2
      with:
        image: ${{ steps.build-image.outputs.image }}
        tags: ${{ steps.build-image.outputs.tags }}
        registry: quay.io/sprungknoedl
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}

  build-hayabusa:
    name: Build and push image
    runs-on: ubuntu-22.04
    steps:
    - name: Check out the repo
      uses: actions/checkout@v4

    - name: Build Image (dagobert-hayabusa)
      id: build-image
      uses: redhat-actions/buildah-build@v2
      with:
        image: dagobert-hayabusa
        tags: latest ${{ github.sha }}
        containerfiles: ./configs/Dockerfile-hayabusa

    - name: Push To quay.io
      id: push-to-quay
      uses: redhat-actions/push-to-registry@v2
      with:
        image: ${{ steps.build-image.outputs.image }}
        tags: ${{ steps.build-image.outputs.tags }}
        registry: quay.io/sprungknoedl
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}

  build-plaso:
    name: Build and push image
    runs-on: ubuntu-22.04
    steps:
    - name: Check out the repo
      uses: actions/checkout@v4

    - name: Build Image (dagobert-plaso)
      id: build-image
      uses: redhat-actions/buildah-build@v2
      with:
        image: dagobert-plaso
        tags: latest ${{ github.sha }}
        containerfiles: ./configs/Dockerfile-plaso

    - name: Push To quay.io
      id: push-to-quay
      uses: redhat-actions/push-to-registry@v2
      with:
        image: ${{ steps.build-image.outputs.image }}
        tags: ${{ steps.build-image.outputs.tags }}
        registry: quay.io/sprungknoedl
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}

  build-timesketch:
    name: Build and push image
    runs-on: ubuntu-22.04
    steps:
    - name: Check out the repo
      uses: actions/checkout@v4

    - name: Build Image (dagobert-timesketch)
      id: build-image
      uses: redhat-actions/buildah-build@v2
      with:
        image: dagobert-timesketch
        tags: latest ${{ github.sha }}
        containerfiles: ./configs/Dockerfile-timesketch

    - name: Push To quay.io
      id: push-to-quay
      uses: redhat-actions/push-to-registry@v2
      with:
        image: ${{ steps.build-image.outputs.image }}
        tags: ${{ steps.build-image.outputs.tags }}
        registry: quay.io/sprungknoedl
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}
